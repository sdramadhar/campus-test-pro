# Custom Domains

Custom domain support stores domain status, DNS TXT verification hash, CNAME guidance, and certificate status. CampusTest Pro does not attempt DNS changes automatically.

Managed TLS integration must be wired in production through the chosen ingress/load-balancer/certificate provider. The default platform domain remains the fallback.
