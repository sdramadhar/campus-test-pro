# White Label Branding

Branding settings include institution name, short name, logo, favicon, colors, login background, email/report logos, footer text, support contact, privacy URL, and terms URL.

The backend checks `custom_branding`, validates color format, blocks script injection, stores version history, and supports private preview/rollback foundation. Arbitrary CSS and JavaScript are not allowed.
